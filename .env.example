# 复制本文件为 .env 后按需修改（.env 勿提交仓库）
#
# 开发：默认使用 vite.config.js 里对 /api 的代理到 localhost:3000，
#       可不设置或保持为空，前端请求仍使用相对路径 /api。
# 生产或直连后端：填写完整 API 根路径（需带 /api 后缀，与后端路由一致）。
#
# VITE_API_BASE=http://localhost:3000/api

VITE_API_BASE=
